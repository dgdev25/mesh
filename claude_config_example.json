{
  "comment": "Example Claude Desktop configuration for Mesh MCP Server",
  "comment2": "Run './run-server.sh -c' to get the exact configuration for your system",
  "comment3": "For platform-specific examples, see the examples/ directory",
  "mcpServers": {
    "mesh": {
      "command": "/path/to/mesh/.mesh_venv/bin/python",
      "args": ["/path/to/mesh/server.py"]
    }
  }
}