{
  "comment": "macOS configuration template — run ./setup.sh to auto-register, or paste this manually",
  "mcpServers": {
    "mesh": {
      "command": "/path/to/mesh/.mesh_venv/bin/python",
      "args": ["/path/to/mesh/server.py"]
    }
  }
}
