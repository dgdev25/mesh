{
  "comment": "Windows-via-WSL configuration template — run ./setup.sh inside WSL to auto-register, or paste this into Claude Desktop config manually",
  "mcpServers": {
    "mesh": {
      "command": "wsl.exe",
      "args": [
        "/path/to/mesh/.mesh_venv/bin/python",
        "/path/to/mesh/server.py"
      ]
    }
  }
}
